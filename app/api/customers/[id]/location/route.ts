import { NextResponse } from 'next/server';import { z } from 'zod';import { getCurrentProfile } from '@/lib/supabase-server';import { adminClient } from '@/lib/supabase-admin';
const schema=z.object({latitude:z.coerce.number().min(-90).max(90),longitude:z.coerce.number().min(-180).max(180)});
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const p=await getCurrentProfile();if(!p||p.role==='cliente')return NextResponse.json({error:'Acesso negado'},{status:403});const{id}=await params;const parsed=schema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Coordenadas inválidas.'},{status:400});const db=adminClient();if(p.role==='vendedor'){const{data}=await db.from('customers').select('seller_id').eq('id',id).single();if(data?.seller_id!==p.id)return NextResponse.json({error:'Empresa fora da sua carteira.'},{status:403});}const{error}=await db.from('customers').update({...parsed.data,location_validated_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq('id',id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true});}
