import {NextResponse} from 'next/server';import {adminClient} from '@/lib/supabase-admin';import {getCurrentProfile} from '@/lib/supabase-server';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const p=await getCurrentProfile();if(!p||p.role==='cliente')return NextResponse.json({error:'Acesso negado.'},{status:403});const{id}=await params;const body=await req.json();const allowed=['enviada','aceita','recusada','expirada'];if(!allowed.includes(body.status))return NextResponse.json({error:'Status inválido.'},{status:400});const db=adminClient();let q=db.from('proposals').update({status:body.status,accepted_at:body.status==='aceita'?new Date().toISOString():null,updated_at:new Date().toISOString()}).eq('id',id);if(p.role==='vendedor')q=q.eq('seller_id',p.id);const{error}=await q;if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true});}
