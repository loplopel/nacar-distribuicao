'use client';
import { useState } from 'react';
export default function CustomerForm({sellers}:{sellers:{id:string,name:string}[]}){
 const[msg,setMsg]=useState(''); const[loading,setLoading]=useState(false);
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const body=Object.fromEntries(new FormData(e.currentTarget).entries());const r=await fetch('/api/admin/customers',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json();setMsg(r.ok?'Cliente cadastrado com sucesso.':j.error||'Erro ao cadastrar.');setLoading(false);if(r.ok){e.currentTarget.reset();setTimeout(()=>location.reload(),600)}}
 return <form className="card form" onSubmit={submit}><div className="card-title"><div><h2>Novo cliente</h2><p>Cadastre a empresa e vincule ao vendedor responsável.</p></div></div>{msg&&<div className={msg.includes('sucesso')?'success':'error'}>{msg}</div>}<div className="form-grid"><label>Nome fantasia<input className="input" name="name" required/></label><label>Razão social<input className="input" name="legal_name"/></label><label>CNPJ<input className="input" name="cnpj"/></label><label>E-mail<input className="input" name="email" type="email"/></label><label>Telefone<input className="input" name="phone"/></label><label>WhatsApp<input className="input" name="whatsapp"/></label><label>Cidade<input className="input" name="city"/></label><label>Estado<input className="input" name="state" maxLength={2}/></label><label>Vendedor<select className="input" name="seller_id"><option value="">Não vinculado</option>{sellers.map(s=><option value={s.id} key={s.id}>{s.name}</option>)}</select></label><label>Condição de pagamento<input className="input" name="payment_terms" placeholder="Ex.: 28/42 dias"/></label><label>Limite de crédito<input className="input" name="credit_limit" type="number" min="0" step="0.01" defaultValue="0"/></label></div><button className="btn btn-primary" disabled={loading}>{loading?'Salvando...':'Cadastrar cliente'}</button></form>
}
